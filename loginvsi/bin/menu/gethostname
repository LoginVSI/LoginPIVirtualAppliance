#!/bin/bash
current_hostname=$(hostname)
current_domainname=$(domainname)
fqdn="$current_hostname.$current_domainname"  
echo "exec:Change the hostname:edit,p:/loginvsi/bin/menu/changehostname ~New hostname (FQDN) ?:${fqdn}~"